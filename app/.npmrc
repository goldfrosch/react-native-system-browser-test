# Flatten node_modules (npm-style) so React Native / Expo native C++ builds
# don't hit Windows path-length limits (CMAKE_OBJECT_PATH_MAX = 250).
# pnpm's default .pnpm/<pkg>@<ver>_<peers>/node_modules/ nesting makes object
# file paths exceed the limit and the Android ninja build fails.
node-linker=hoisted
